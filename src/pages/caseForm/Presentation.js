import React, { useEffect } from "react";
import Header from "../../components/header/Header"

const Presentation = ({}) => {
  return(
    <div className="className="homeContainer>
      <Header />
    </div>
  )
}

export default Presentation;